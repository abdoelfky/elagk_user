import 'package:flutter/material.dart';

class AppBarLogoutIcon extends StatelessWidget {
  const AppBarLogoutIcon({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Icon(
      Icons.exit_to_app_rounded,
      color: Colors.black,
    );
  }
}
